# Wallet directory

`bot-keypair.json` is intentionally **not** included. It contains a private key and is ignored by Git.

For PAPER mode you do not need a wallet. For optional LIVE mode, read `docs/LIVE_TRADING.md`.
